# vercel deploy uploads the working directory and reads THIS file, not
# .gitignore. Without it the CLI ships the 236 MB of source footage and the
# deploy dies on the 100 MB per-file limit. Only site/ is ever served.
/Videos & Images/
/Fonts/
/Template Shared by the Client/
/Dropzone_/
.DS_Store
